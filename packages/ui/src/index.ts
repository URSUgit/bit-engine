// Shared UI component library — components live here and are consumed by apps/web
// and any future apps in the monorepo.
//
// Convention: one file per component, named after the component.

export { Badge } from "./badge";
export { Button } from "./button";
export { Card } from "./card";
export { Spinner } from "./spinner";
